This project is a precursor to a larger webscraping project I want to undertake.

Currently, it is ran using "npm start webAddress" in the terminal

When ran, it looks through that web page for links to other pages on the same domain then continues iteratively and publishes a report on the number of connections to each site it accessed.
